# Server inspector
Testing functionality of an MCP server is important, and needed even without connectin to a full app.
The python MCP sdk supports an in-browser inspector to test the server in real time:
```bash
mcp dev mcp_server.py
```